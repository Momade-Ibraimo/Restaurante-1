import { NavLink, Outlet } from 'react-router-dom'
import { Clock3, LogOut, MapPinned, UserRound } from 'lucide-react'
import { restaurantConfig } from '../../config/restaurant'
import { useAuthStore } from '../../stores/useAuthStore'
export function CourierLayout(){ const user=useAuthStore((s)=>s.user);const logout=useAuthStore((s)=>s.logout);return <div className="mx-auto min-h-screen max-w-xl bg-[#f5f4f0] pb-20 shadow-xl"><header className="sticky top-0 z-20 bg-ink px-5 py-4 text-white"><div className="flex items-center justify-between"><div><p className="font-display text-lg font-bold">{restaurantConfig.restaurantName}</p><p className="text-xs text-stone-400">Entregas</p></div><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-emerald-400"/><span className="text-xs font-bold">Disponível</span></div></div></header><main><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-20 mx-auto flex h-18 max-w-xl items-center justify-around border-t border-stone-200 bg-white"><NavLink end to="/delivery" className={({isActive})=>`grid justify-items-center gap-1 text-xs font-bold ${isActive?'text-brand-600':'text-stone-400'}`}><MapPinned size={21}/>Entregas</NavLink><NavLink to="/delivery/history" className={({isActive})=>`grid justify-items-center gap-1 text-xs font-bold ${isActive?'text-brand-600':'text-stone-400'}`}><Clock3 size={21}/>Histórico</NavLink><button onClick={logout} className="grid justify-items-center gap-1 text-xs font-bold text-stone-400"><LogOut size={21}/>Sair</button><div className="grid justify-items-center gap-1 text-xs font-bold text-stone-400"><UserRound size={21}/>{user?.name?.split(' ')[0]||'Carlos'}</div></nav></div> }
